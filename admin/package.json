{
  "name": "telegram-checkin-admin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=dot tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
