# Avatar permissions

- Every authenticated user can choose, crop, replace, or remove their own personal avatar.
- Personal avatar selection is not restricted to administrators.
- The platform-wide brand avatar remains administrator-only because it changes the shared interface for every user.
- A user's personal avatar is isolated to that user's profile and does not replace another user's avatar.
